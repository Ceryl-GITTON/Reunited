name: reunited_countdown
description: Reunited Countdown - Web Version
version: 1.4.0+1
publish_to: 'none'

environment:
  sdk: '>=3.1.4 <4.0.0'
  flutter: ">=3.13.0"

dependencies:
  flutter:
    sdk: flutter
  
dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
